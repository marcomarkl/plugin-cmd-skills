{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *SKILL.md) echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Eine SKILL.md wurde geaendert. Pruefe, ob die Version in cmd/.claude-plugin/plugin.json angehoben werden muss.\"}}';; esac"
          }
        ]
      }
    ]
  }
}
